# Ensure shell scripts always use LF line endings
*.sh    text eol=lf

# Ensure Windows batch scripts always use CRLF
*.bat   text eol=crlf

# Normalize all other text files on checkout
*       text=auto
